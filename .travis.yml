language: python
python:
    - 3.6
install:
    - pip install boto3
    - pip install utils
    - pip install moto
    - pip install coverage

script: 
    - python dataProcessor.py
    - sonar-scanner
